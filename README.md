# 3D-Mapping-Search
3D Search History (The "Mapping" Idea) "building a 3D landscape of a user's search timeline. He would use Python to process the search logs on the backend, and then use TypeScript with Three.js (a 3D library) to render the interactive map in the browser.
